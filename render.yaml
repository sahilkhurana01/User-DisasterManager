services:
  # Frontend Service
  - type: web
    name: disaster-management-frontend
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_ENV
        value: production
